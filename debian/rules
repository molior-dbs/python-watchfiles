#!/usr/bin/make -f

export PYBUILD_NAME = python3-watchfiles

%:
        dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_build:
        # ltrace -e getenv cargo
        cargo build
        dh_auto_build

override_dh_auto_test: